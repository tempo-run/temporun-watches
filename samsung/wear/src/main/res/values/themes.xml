<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base do app Wear OS. O Compose define as cores em runtime;
         aqui só garantimos fundo preto (OLED) e sem action bar. -->
    <style name="Theme.TempoRunWear" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:colorBackground">@color/black</item>
    </style>
</resources>
